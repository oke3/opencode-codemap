{
  "name": "my-cli-tool",
  "version": "1.0.0",
  "bin": {
    "my-cli": "dist/cli.js"
  },
  "main": "dist/index.js",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  }
}
